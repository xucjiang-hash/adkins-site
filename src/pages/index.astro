---
const siteTitle = "Brian与Adkins的生活手册";
---

<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{siteTitle}</title>

    <style>
      :root{
        --text:#111;
        --muted:#666;
        --border:#e5e5e5;
        --bg:#fff;
        --hover:#f0f0f0;
      }

      *{ box-sizing: border-box; }

      body{
        margin:0;
        font-family: system-ui, -apple-system, Segoe UI, Roboto, "PingFang SC", "Microsoft YaHei", sans-serif;
        color:var(--text);
        background:var(--bg);
      }

      /* ===== Top nav ===== */
      .nav-wrap{
        position: sticky;
        top: 0;
        z-index: 50;
        background: rgba(255,255,255,.92);
        backdrop-filter: blur(10px);
        border-bottom: 1px solid var(--border);
      }

      nav{
        max-width: 980px;
        margin: 0 auto;
        padding: 12px 18px;
        display:flex;
        align-items:center;
        gap: 18px;
      }

      .nav-links{
        margin-left: auto;
        display:flex;
        align-items:center;
        gap: 18px;
        font-size: 14px;
      }

      .nav-links a.toplink{
        color: var(--muted);
        text-decoration:none;
        padding: 8px 10px;
        border-radius: 10px;
        display: inline-flex;
        align-items: center;
      }

      .nav-links a.toplink:hover{
        color: var(--text);
        background: #f6f6f6;
      }

      .nav-links a.toplink.active{
        color: #d11;
        font-weight: 600;
      }

      /* ===== Click dropdown: NEVER hover-based ===== */
      details.dd{
        position: relative;
      }

      details.dd > summary{
        list-style: none;
        cursor: pointer;
        user-select: none;

        font: inherit;
        color: var(--muted);
        background: transparent;
        border: 0;
        padding: 8px 10px;
        border-radius: 10px;

        display: inline-flex;
        align-items: center;
        gap: 6px;
      }

      details.dd > summary::-webkit-details-marker{
        display:none;
      }

      details.dd > summary:hover{
        color: var(--text);
        background: #f6f6f6;
      }

      details.dd > summary:focus-visible{
        outline: 2px solid rgba(209, 17, 17, 0.25);
        outline-offset: 2px;
      }

      /* 菜单层：默认不可见不可点；open 才可见可点 */
      .dd-menu{
  position:absolute;
  top: calc(100% + 10px);
  left: 0;                 /* ✅ 让它贴着按钮左边对齐 */
  width: max-content;
  min-width: 260px;

  background: #fff;
  border: 1px solid var(--border);
  border-radius: 14px;
  padding: 8px;
  box-shadow: 0 18px 40px rgba(0,0,0,.14);

  opacity: 0;
  transform: translateY(-6px);
  pointer-events: none;

  transition: opacity 140ms ease, transform 140ms ease;
  z-index: 1000;
}

details.dd[open] > .dd-menu::before{
  content:"";
  position:absolute;
  top: -8px;
  left: 18px;                      /* 尖角位置：对齐按钮文字 */
  width: 14px;
  height: 14px;
  background: #fff;
  border-left: 1px solid var(--border);
  border-top: 1px solid var(--border);
  transform: rotate(45deg);
}

details.dd > summary { list-style: none; }
details.dd > summary::marker { content: ""; }

      details.dd[open] > .dd-menu{
        opacity: 1;
        transform: translateY(0);
        pointer-events: auto;
      }

      /* Menu groups */
      .menu-group{
        padding: 4px;
      }

      .menu-title{
        padding: 10px 12px;
        border-radius: 12px;
        color: var(--text);
        font-weight: 650;
        font-size: 14px;
        line-height: 1.3;
      }

      .menu-title .desc{
        display:block;
        margin-top: 2px;
        font-size: 12px;
        font-weight: 600;
        color: var(--muted);
      }

      /* 子项（入口） */
      .menu-sub{
        display:block;
        width: 100%;
        padding: 10px 12px 10px 22px;
        border-radius: 12px;
        color: var(--text);
        text-decoration:none;
        font-size: 14px;
        font-weight: 400;
        line-height: 1.4;

        cursor: pointer !important;   /* ✅ 强制手掌 */
        transition: background 120ms ease;
      }

      .menu-sub:hover{
        background: var(--hover);
      }

      .menu-sub:focus-visible{
        outline: 2px solid rgba(209, 17, 17, 0.28);
        outline-offset: 2px;
        background: var(--hover);
      }

      /* ===== Hero ===== */
      .container{
        max-width: 980px;
        margin: 0 auto;
        padding: 34px 18px 60px;
      }

      h1{
        margin: 28px 0 28px;
        text-align:center;
        font-size: 56px;
        line-height: 1.05;
        letter-spacing: -0.02em;
      }

      .hero-img{
        width: 100%;
        border-radius: 10px;
        overflow: hidden;
        border: 1px solid var(--border);
        background: #fafafa;
      }

      .hero-img img{
        width: 100%;
        height: auto;
        display: block;
      }

      .hero-caption{
        margin: 30px auto 0;
        text-align: center;
        font-size: 18px;
        color: #777;
        letter-spacing: 0.06em;
        font-weight: 300;
      }

      @media (max-width: 640px){
        h1{ font-size: 34px; }
        .nav-links{ gap: 8px; }
      }
    </style>
  </head>

  <body>
    <header class="nav-wrap">
      <nav>
        <div class="nav-links">
          <a class="toplink active" href="/">主页</a>

          <!-- Brian-Adkins电影大奖 -->
          <details class="dd">
            <summary>Brian-Adkins电影大奖 ▾</summary>
            <div class="dd-menu" role="menu" style="min-width:320px;">
              <div class="menu-group">
                <div class="menu-title">
                  历届 Brian-Adkins 电影大奖
                  <span class="desc">获奖与年度观影推荐</span>
                </div>
                <a class="menu-sub" href="/awards/2022">第一届（2021-2022）</a>
                <a class="menu-sub" href="/awards/2023">第二届（2022-2023）</a>
                <a class="menu-sub" href="/awards/2024">第三届（2023-2024）</a>
                <a class="menu-sub" href="/awards/2025">第四届（2024-2025）</a>
              </div>
            </div>
          </details>

          <!-- 电影榜单 -->
          <details class="dd">
            <summary>电影榜单 ▾</summary>
            <div class="dd-menu" role="menu" style="min-width:320px;">
              <div class="menu-group">
                <div class="menu-title">
                  榜单
                  <span class="desc">个人精选电影榜单</span>
                </div>
                <a class="menu-sub" href="/movies/top1000">千佳影片</a>
                <a class="menu-sub" href="/movies/top150">Top150导演</a>
                <a class="menu-sub" href="/movies/top50">21世纪五十佳华语影片</a>
              </div>
            </div>
          </details>

          <!-- 年度书单 -->
          <details class="dd">
            <summary>年度书单 ▾</summary>
            <div class="dd-menu" role="menu" style="min-width:260px;">
              <div class="menu-group">
                <div class="menu-title">
                  年度书单
                  <span class="desc">每年阅读记录与推荐</span>
                </div>
                <a class="menu-sub" href="/books/2020">2020</a>
                <a class="menu-sub" href="/books/2021">2021</a>
                <a class="menu-sub" href="/books/2022">2022</a>
                <a class="menu-sub" href="/books/2023">2023</a>
                <a class="menu-sub" href="/books/2024">2024</a>
                <a class="menu-sub" href="/books/2025">2025</a>
              </div>
            </div>
          </details>

          <!-- 旅行日志 -->
          <details class="dd">
            <summary>旅行日志 ▾</summary>
            <div class="dd-menu" role="menu" style="min-width:260px;">
              <div class="menu-group">
                <div class="menu-title">
                  旅行
                  <span class="desc">足迹与名录</span>
                </div>
                <a class="menu-sub" href="/travel/china">国内足迹</a>
                <a class="menu-sub" href="/travel/international">世界足迹</a>
                <a class="menu-sub" href="/travel/5A">5A景区名录</a>
                <a class="menu-sub" href="/travel/UNESCO">世界遗产名录</a>
              </div>
            </div>
          </details>
        </div>
      </nav>
    </header>

    <main class="container">
      <h1>{siteTitle}</h1>

      <div class="hero-img">
        <img src="/hero.JPG" alt="主页主图" />
      </div>

      <p class="hero-caption">所谓生活，无非时间、空间和你。</p>
    </main>

    <!-- 稳定性增强：打开一个就关其它；点空白处关闭；Esc 关闭 -->
    <script is:inline>
      (() => {
        const dropdowns = Array.from(document.querySelectorAll('details.dd'));

        dropdowns.forEach(d => {
          d.addEventListener('toggle', () => {
            if (d.open) dropdowns.forEach(x => { if (x !== d) x.open = false; });
          });
        });

        document.addEventListener('click', (e) => {
          const t = e.target;
          const inside = dropdowns.some(d => d.contains(t));
          if (!inside) dropdowns.forEach(d => d.open = false);
        });

        document.addEventListener('keydown', (e) => {
          if (e.key === 'Escape') dropdowns.forEach(d => d.open = false);
        });
      })();
    </script>
  </body>
</html>
